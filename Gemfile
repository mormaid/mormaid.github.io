# Gemfile
# This tells Bundler which Ruby gems this site needs.
# GitHub Actions will read this file, install these gems, build the Jekyll site, and deploy the finished static HTML/CSS to GitHub Pages.

source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-feed"
gem "jekyll-seo-tag"

# Needed for local Jekyll serving on newer Ruby versions.
# It is harmless in GitHub Actions.
gem "webrick"